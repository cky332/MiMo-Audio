# MiMo-Audio 压力测试实验报告：真实环境与新任务设定下的 failure cases

> 前置工作见 `PAPER_NOTES.md`（论文复现笔记）。本报告是第二阶段：把论文的方法放到
> 更真实的使用条件下，系统性地暴露问题。5 条实验线、24 个实验，全部可在 CPU 复跑
> （`reproduction/experiments/`，逐条命令见文末）。
>
> **方法边界（先说清楚）**：本环境无 GPU、官方 tokenizer 硬依赖 flash-attn、HuggingFace
> 被网络策略阻断，因此**无法加载官方 7B 权重**。对策分三层，每层的证据强度不同：
> ① 官方 LM 代码（`modeling_mimo_audio.py` 等）不依赖 flash-attn，**以 tiny 配置实例化
> 官方类、驱动官方真实生成循环**——这些结论对官方代码**直接成立**；
> ② 用仓库自带的**真实语音**（ESD 情感语音 + 中英提示音 + 56 秒对话录音）训练小规模
> 复现 tokenizer，测**流水线机制**（分段、补齐、OOD）——结论对"机制方向"成立，
> 绝对数值不外推到 1.2B 模型；
> ③ 受控合成数据 + 已知信息论下界的训练探针，测**架构本身**——结论对架构结构性质
> 成立，量级随模型规模变化。

---

## 结果一览

| # | 实验 | 结果 | 严重度 |
|---|---|---|---|
| E1.1 | 官方 top-k 采样 batch>1 | **崩溃**；词表恰好等于 batch 时**静默采错** | 高（若做批量评测） |
| E1.3 | 官方停止检测 | 只查一个位置；批处理只看第 0 行 | 中 |
| E1.4a | fp32 加载官方模型 | 能跑：bf16 缓冲静默降精度（推翻此前静态分析结论） | 低 |
| E1.4c | 默认参数 generate() | AttributeError 崩溃 | 中 |
| E1.4d | batch=2 生成 | 不崩溃但**第 1 行的模态分支被第 0 行决定**——静默错误 | 高（若做批量推理） |
| E1.5 | 官方 delay 调度 | 与论文 Eq.15 阶梯**逐调用吻合**（16/16） | ✓ 验证 |
| E1.6 | 官方 patch encoder 因果性 | 默认配置下**实测为因果**（Δ=0），与论文"双向"矛盾坐实 | 中 |
| E1.8 | 停止粒度 | 音频只能在 160ms patch 边界结束；`<|eostm|>` 本身再吞 160ms | 低 |
| E2.2 | 60s 无重叠分段**编码** | 码流不一致是**全局的**：深层码本 22–38% 不一致，边界桶最高 37.9% | 高（长音频） |
| E2.3 | 60s 无重叠分段**解码** | 接缝处 MSE 2.7×、拼接点跳变 16× 于正常步长；causal decoder 丢左上下文 | 高（>60s 生成） |
| E2.4 | group_size 补齐 | 15 条真实语音平均 **+85ms**（最高 +160ms）冻结帧尾巴；时长量化到 160ms 网格 | 中 |
| E2.5 | 官方长度公式 | 与真实卷积算术 0 失配（1–199 全扫描） | ✓ 验证 |
| E3.0 | RVQ 无稳定器训练 | **全码本坍缩到 1 个码**；官方代码里论文未提的 kmeans+死码替换是成败关键 | 高（复现者） |
| E3.1 | OOD 信号 | 非语音（噪声/纯音/和弦/扫频）**全部坍缩到 ~2 个码**；-40dB 语音"失聪"（98% 单码） | 高（通用音频场景） |
| E4.1 | patch 边界瓶颈 | 每 patch 第 0 帧 NLL **2.82 vs 1.23**（下界 1.10）：每 160ms 一次周期性不确定尖峰 | 中（结构性） |
| E4.2 | delay 消融 | 有 delay：跨码本 NLL≈0.01；无 delay：2.78（彻底失败）→ delay 设计被定量证成 | ✓ 验证 |
| E4.3 | 损失权重 100:1 | 文本 NLL 几乎无收益（0.475 vs 0.480），音频 NLL 代价 +8% | 低（小规模） |
| E4.4 | A2T 语义目标 vs 重建 | **反直觉：λ=10 反而改善重建**（3.17 vs 4.13）——支持论文的联合训练主张 | ✓ 有利证据 |
| E4.5 | 双向 vs 因果 patch encoder | 1.640 vs 1.636：**几乎无差别**——论文-代码矛盾实际影响很小 | ✓ 澄清 |
| E5.1 | 上下文预算 | **~14–15 分钟对话打满 8192 上下文**（与轮长无关），且无任何截断机制 | 高（长对话） |
| E5.3 | 部署链路 | 明明实现了带重叠的 streaming_decode，`forward()` 却用无重叠分段解码 | 中 |

---

## Track 1：官方代码的行为学测试（tiny 配置实例化官方类，CPU 直跑）

官方 LM 栈只依赖 torch+transformers，可以在 CPU 上以 tiny 配置构造
`MiMoAudioForCausalLM` 并驱动**官方原版**生成循环。以下全部是对官方代码的直接观测
（`exp_track1_official.py`，完整输出在 `experiments/artifacts/track1_results.txt`）。

**E1.1 top-k 的广播 bug 有两种表现。** `torch.topk(scores, k)[0][:, -1]` 缺 keepdim：
B=2/V=100 时直接 `RuntimeError`；而当词表大小恰好等于 batch 数时（例如二分类打分场景）
**不崩溃但静默采错**——构造 `[[0,5],[9,0]]` 的 top-1 采样，正确答案 `[1,0]`，官方实现
返回 `[0,0]`。后者比崩溃更危险。

**E1.4d 批量生成的静默污染。** batch=2 生成不报错，但 `next_text_tokens[0]` 使
**整个 batch 的"说话/写字"分支由第 0 行决定**：实验中第 0 行选了文本分支，第 1 行
即使其 argmax 是语音 token 也被迫填充空音频。做批量评测（例如刷 benchmark）时若不
逐条跑，结果会被静默污染——结合 E1.1，这解释了为什么官方所有接口都写死 batch=1。

**E1.4a 一个被推翻的静态分析结论（诚实记录）。** 此前审计（含本人笔记初稿的判断）认为
`_prepare_input_embeds` 硬编码 bf16 会让 fp32 加载崩溃。实测：**fp32 能跑**——fp32
embedding 被就地降精度写入 bf16 缓冲（同类浮点允许 cast），RMSNorm 又把激活提回
fp32。真实问题从"崩溃"降级为"静默精度挤压"。跑真实代码比读代码可靠。

**E1.5 delay 机制实现的正字验证。** 用记录器采样器（每通道不同词表大小作指纹）逆向
官方 `local_forward` 的发射调度：16 次采样调用的通道序列与论文 Eq.15 的阶梯
`t0:[c0] t1:[c0,c1] … t6:[c3]` **逐位吻合**，且空 token 被硬禁。官方实现与论文公式
在此处一致（也与我们的复现互相印证）。

**E1.6 "双向"之争的实测判决。** 对官方 `apply_input_local_transformer` 做扰动实验：
默认配置（`input_full_attention=None`）下，扰动 patch 内第 3 帧对第 0 帧编码的影响
**Δ=0.000000（严格因果）**；显式设 `True` 后 Δ=0.0117（双向）。论文 2.2.1 的
"bidirectional" 与代码默认行为的矛盾从静态阅读升级为实测事实。（其实际影响见 E4.5。）

**E1.3/E1.8 停止机制的粒度与盲区。** `MiMoStopper` 只检查最后一个 patch 的第 0 帧：
停止 token 出现在倒数第二个 patch 时返回 False（正常循环中不会发生，但任何绕过逐 patch
检查的用法都会失控）；batch 下只看第 0 行。停止粒度：音频只能在 160ms 边界结束，且
`<|eostm|>` 自己还要消耗一整个 patch 的空音频帧。

**E1.7 数据格式类的边界。** 官方 `InputSegment` 用 `-100` 而非空 token 做组内填充
（与生成侧的"重复填充"是两套并存的约定）；音频长度不是 group_size 倍数时裸
`AssertionError`（上游靠重复末帧补齐兜底）；`StreamingInputSegment` 空文本+音频不报错。
交错模式实测：`sostm | 5T 5A 5T 3A | 残余文本+eot | eostm`，与代码注释一致。

## Track 2：分段流水线在长音频上的伪影（真实 56 秒录音）

官方推理在两处用**无重叠独立分段**：编码切 60s mel 段（mimo_audio.py:258-266），解码切
60s 码段（:1150-1155）。用仓库的 56 秒真实对话录音在 1/12 尺度（5s 段）复现该机制
（`exp_track2_segmentation.py`）：

**E2.2 分段编码：token 流依赖切块方式，且是全局性的。**

| 距接缝距离（token） | q0 不一致 | q0–3 平均不一致 | 特征相对变化 |
|---|---|---|---|
| 0–1（≤80ms） | 3.0% | **37.9%** | 7.7% |
| 2–4 | 0.0% | 21.6% | 2.8% |
| 5–12 | 2.8% | 24.6% | 3.0% |
| 13–39 | 1.7% | 23.7% | 2.7% |
| ≥40（内部） | 4.6% | 27.5% | 2.8% |

关键不是边界桶最高（37.9%），而是**内部也有 22–28% 的深层码不一致**：encoder 是全段
双向注意力，每个 token 的特征依赖整段音频，切块改变所有 token 的上下文。推论：
**同一段音频的"token 表示"不是唯一的**——它依赖分段策略。对 few-shot prompt（示例逐条
编码 vs 拼接编码）、码流缓存、训练-推理一致性都有实际影响。浅层（q0）稳健得多（<5%），
这与"信息集中在前两层大码本"的设计一致。

**E2.3 分段解码：接缝可测且必然。** 全段解码 vs 分段拼接：接缝 100ms 窗口的 MSE 是
内部的 **2.7 倍**，拼接点样本跳变 0.036，是正常相邻样本步长（0.0022）的 **16 倍**
（阶跃不连续，听感上是每 60 秒一次的咔哒/相位断裂）。另外因为 decoder 是因果的，
每个分段丢失全部左侧历史，导致分段解码与全段解码在**整段上**都有偏差（overall mel-L1
0.42）。注意：tokenizer 里明明实现了带 10s/1.6s 重叠的 `streaming_decode`，但
`forward()` 没有用它——工程上是已知问题、未接线。

**E2.4 补齐失真：每条语音尾部平均 +85ms 冻结帧。** 官方 `preprocess_input` 把 token 数
补齐到 4 的倍数的方式是**重复最后一帧**。15 条真实语音实测：补 0/1/2/3 帧的分布为
3/6/3/3，时长变化 +21ms 到 +160ms（平均 +85ms）。所有输入音频时长都被量化到 160ms
网格，尾帧是冻结频谱——对韵律边界敏感的任务（如语速转换的 few-shot 示例）是系统性噪声。

**E2.5 长度公式验证（通过）。** `get_output_length` 与真实卷积算术在 mel 长度 1–199
全扫描下 0 失配。

## Track 3：域外信号——"通用音频模型"的 tokenizer 在非语音上的行为

**E3.0 先于 OOD 的发现：论文没写的两个稳定器决定训练成败。** 论文 2.1 对 RVQ 训练只
写了 commit loss；官方 `quantization.py` 里还有 kmeans 初始化和死码替换
（`threshold_ema_dead_code=10`）。消融（150 步，同数据同种子）：

| 配方 | 重建损失 | q0–3 码本熵 | 唯一码数 |
|---|---|---|---|
| 纯 EMA-VQ（按论文文字实现） | 5.23 | 全部 ≈0 bit | **1 / 1 / 1 / 1** |
| 官方配方（kmeans+死码替换） | **4.44** | 1.3–4.0 bit | 4 / 30 / 15 / 12 |

按论文文字实现的 RVQ **在真实语音上直接坍缩**（每层只剩 1 个码）。这两个机制只存在于
代码里——"照论文复现"和"照代码复现"是两个不同的模型。

**E3.1 OOD 信号套件（tiny tokenizer，真实语音训练，held-out 为未见过的说话人）：**

| 信号 | 重建退化 | q0 熵 | 单码占比 |
|---|---|---|---|
| held-out 语音（基线） | 1.00× | 1.85 bit | 49% |
| 语音削波 5× | 1.12× | 2.24 bit | 35% |
| 语音 +DC 偏置 | 1.12× | 1.85 bit | 49% |
| 16k 当 24k 播（采样率错配） | 1.03× | 1.41 bit | 65% |
| **语音 −40dB** | 2.14× | **0.14 bit** | **98%** |
| 白噪声 | 2.78× | 0.14 bit | 98% |
| 粉噪声 | 1.07× | 0.28 bit | 96% |
| 纯音 440Hz | 2.96× | 0.14 bit | 98% |
| 扫频 0.1–8kHz | 1.97× | 0.14 bit | 98% |
| 三音和弦 | 3.56× | 0.14 bit | 98% |
| 静音 | 15.5×* | 0.14 bit | 98% |

（*静音的 mel-L1 有对数刻度放大成分，看熵列更公平）

两个模式：① **非语音内容整体坍缩到 ~2 个码**——tokenizer 把噪声、乐音、扫频统统映射成
同一种"非语音"码，下游 LM 从码流里**根本看不到这些信号的区别**。这与论文第 6 节自认的
"复杂声学场景/音乐弱"给出了机制层面的解释路径：瓶颈可以在 tokenizer 就发生。
② **音量鲁棒性缺口**：−40dB 的语音（远场/小声说话）码流与静音几乎无异——模型对安静
语音"失聪"。削波、DC、采样率错配反而处理得不错。
（规模注记：1.2B tokenizer 训练分布远宽于此，坍缩程度必然更轻；本实验证明的是
**失效方向与检测方法**——码本熵是比重建损失更灵敏的域外探测器。）

## Track 4：受控训练探针——架构在已知下界任务上的损耗

合成数据 + 已知熵下界，端到端训练复现架构（`exp_track4_architecture.py`）：

**E4.1 patch 边界是周期性的建模弱点。** 各通道独立 ±1 随机游走（每帧条件熵 = ln3 =
1.099 nats）。训练后按帧位置分解 NLL：

| patch 内帧位置 | NLL | 超出下界 |
|---|---|---|
| 帧 0（只经 LLM 瓶颈条件） | **2.821** | +1.722 |
| 帧 1 | 1.230 | +0.131 |
| 帧 2 | 1.237 | +0.139 |
| 帧 3 | 1.232 | +0.133 |

帧 0 的超额是其余帧的 **12 倍**（+145% of floor）。结构原因：patch decoder 生成帧 0 时
只有一个降维后的 LLM 隐向量可用（E5.2：容量上足够 66×，所以是**表示瓶颈**而非信息瓶
颈），帧 1–3 则直接看到 patch 内历史。推论：**每 160ms 一次的周期性不确定尖峰**是该
架构的固有属性，预测生成语音在 patch 边界处更易出现细粒度伪影（7B 下量级会缩小，
但不对称性是结构性的）。

**E4.2 delay 机制的定量证成（通过）。** 同帧跨码本依赖数据（c_r = c_0+r mod 16）：
delay (0,1,2,3) 时 ch1–3 NLL ≈ 0.01（完美建模）；delay (0,0,0,0) 时 2.78 ≈ ln16
（完全失败）。MusicGen 式 delay 不是装饰，是该类分层生成的必要件。

**E4.3 100:1 损失权重的代价。** 相同交错数据、相同步数：paper 权重 vs 均匀权重的文本
NLL 0.475 vs 0.480（无差别），音频 NLL 1.574 vs 1.453（**+8% 代价**）。小规模上 100:1
没换来文本收益、只付了音频代价——它的真实作用（保住 7B 预训练文本能力不被冲刷）在
toy 上无法体现，但代价侧是实测的。

**E4.4 反直觉：A2T 语义目标改善了重建。** λ_A2T=10 的重建损失 3.17 **优于** λ=0 的
4.13（250 步、同种子、真实语音、说话人身份作 toy 转写目标）。与"语义-声学冲突"的
直觉相反，辅助语义梯度在小规模上起了正则/塑形作用。这是**支持论文核心主张**
（联合语义训练不必牺牲重建）的证据——如实记录。

**E4.5 双向 vs 因果之争的实际影响：≈0。** 音频 NLL 1.640 vs 1.636。patch 只有 4 帧，
注意力方向在这么短的窗口里几乎不改变建模能力。论文-代码矛盾（E1.6）成立，但其实际
后果很小——属于文档错误而非设计缺陷（精确复现 checkpoint 行为时仍需照代码）。

## Track 5：部署算术——论文没写的预算表

**E5.1 上下文预算：约 14–15 分钟对话是硬上限。** 1 个 LLM 位置 = 160ms 音频或 1 个
文本 token；上限 8192 位置 = 纯音频 21.8 分钟。按官方对话模板算完整轮次
（脚手架+用户音频+助手转写交错+音频）：

| 轮型 | 位置/轮 | 打满轮数 | 对话时长 |
|---|---|---|---|
| 5s 问 + 10s 答 | 149 | 54 | ~14 min |
| 10s 问 + 20s 答 | 277 | 29 | ~14 min |
| 30s 问 + 60s 答 | 792 | 10 | ~15 min |

官方代码没有任何截断/滑窗/摘要机制——`self.history` 无限拼接，到 8192 直接停止生成。
多轮 message_list 模式还会**每轮重放并重新编码全部历史音频**（第 20 轮时累计重复编码
~190 倍单轮成本）。语音-文本汇率：说一句话（~4s）花 25 位置，打出来只花 15 位置——
语音模态在上下文里贵 67%。

**E5.2 patch 条件向量容量。** 每 patch 内容 248 bit vs 1024 维 bf16 条件向量原始容量
16384 bit（66×）——瓶颈是表示性的（E4.1 实测了它的真实损耗），不是信息论的。

**E5.3 机制齐备但未接线。** 带 10s/1.6s 重叠的 `streaming_decode` 在 tokenizer 里实现
了、且 overlap 参数与 vocoder 感受野精确对齐，但推理主路径 `forward()` 用的是无重叠
60s 分段——E2.3 的接缝在部署路径上是既成事实。

---

## 综合：新发现的 failure cases（按真实使用场景组织）

1. **长音频/长回复（>60s，正是论文引以为傲的 talk show / 直播续写场景）**：
   编码端 token 流依赖切块方式（深层码 22–38% 不一致）+ 解码端每 60s 一次硬接缝
   （拼接点跳变 16×）。修复方向明确（官方自己的 streaming_decode 已有重叠机制）。
2. **长对话（>15 分钟）**：上下文预算耗尽且无任何管理机制，多轮重放开销二次增长。
3. **批量部署/评测**：top-k 广播 bug（崩溃或静默采错）+ 模态分支被第 0 行劫持
   （静默污染）——所有批量结果必须逐条跑才可信。
4. **通用音频输入（音乐/环境音/远场小音量）**：tokenizer 码本对非语音坍缩、对 −40dB
   语音"失聪"的失效方向；码本熵是灵敏的域外探测器，建议作为部署侧监控指标。
5. **韵律敏感任务**：所有输入被补齐到 160ms 网格且尾部平均 +85ms 冻结帧；音频只能在
   160ms 边界处停止。
6. **照论文复现训练的人**：纯按论文文字实现 RVQ 会直接码本坍缩——kmeans 初始化与
   死码替换只在代码里；empty token 索引、patch encoder 因果性同理（后者实测影响≈0）。
7. **结构性弱点（规模不能消除，只能缩小）**：每 160ms 的 patch 第 0 帧不确定尖峰
   （+145% 下界超额 @tiny 规模）。

同时有四项**正面验证**应当如实列出：官方 delay 实现与论文公式逐位一致（E1.5）；
delay 设计被消融定量证成（E4.2）；A2T 联合目标在小规模上与重建**协同**而非冲突
（E4.4，支持论文核心主张）；长度公式与 top-p 实现无误（E2.5/E1.2）。

## 局限声明

- tiny 规模（万级参数）+ ~100 秒真实语音训练：Track 2/3/4 的**绝对数值**不能外推到
  1.2B/7B；能外推的是机制方向、失效模式与检测方法。
- Track 1 是对官方代码的直接观测，结论不受规模限制，但 tiny 配置未覆盖
  checkpoint config 可能改变的开关（如 `input_full_attention` 的实际值）。
- 未做：官方权重上的行为评测（网络与硬件边界）、真实音乐/环境音语料（仓库无此类
  样本，合成信号代理）、多说话人重叠语音。

## 复跑方式

```bash
cd reproduction/experiments
python exp_train_tokenizer.py        # ~3 min，训练并缓存真实语音 tiny tokenizer
python exp_track1_official.py        # 官方代码行为学（秒级）
python exp_track2_segmentation.py    # 分段伪影
python exp_track3_ood.py             # OOD + 码本坍缩消融
python exp_track4_architecture.py    # 架构探针（~6 min）
python exp_track5_budget.py          # 预算算术（秒级）
```

各实验原始输出存于 `reproduction/experiments/artifacts/track*_results.txt`。
